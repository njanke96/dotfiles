git:
  pagers:
    - pager: delta --paging=never --line-numbers
      colorArg: always
    - externalDiffCommand: difft --color=always --display=inline --context=10
os:
  editPreset: 'helix (hx)'
  openLink: ^open {{link}}
